POP_deus_vult = {

	POP_piuss_wall_dec = {
		fire_only_once = yes
		allowed = { has_idea = piuss_wall }
		visible = {
			NOT = {
				has_completed_focus = POP_defund_the_wall
			}
		}
		available = {
			bunker > 24
		}
		activation = {
			has_completed_focus = POP_deus_vult
			NOT = {
				has_completed_focus = POP_defund_the_wall
			}
		}
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 200
		timeout_effect = {
			add_stability = -0.15
		}
		complete_effect = {
			add_political_power = 150
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
	POP_fortify_the_border = {
		fire_only_once = no
		visible = {
			has_completed_focus = ITE_for_italy_and_italians
			NOT = {
				has_country_flag = ite_civil_war
			}
		}
		cost = 50
		days_re_enable = 40
		available = {	} 
		complete_effect = {
		    if = {
				limit = {
					check_variable = { ite_voluntary_cons > 4 }
				}
				country_event = { id = ite.100 days = 1  }
			}
			add_to_variable = { ite_voluntary_cons = 1 }
			add_manpower = 25000
		}
		
		ai_will_do = {
			factor = 10
		}
	}
	
	
	
}