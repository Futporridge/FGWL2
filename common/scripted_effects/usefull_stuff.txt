transfer_all_unit_leaders_to_ROOT = {
	every_unit_leader = {
		limit = { has_unit_leader_flag = CHI_original_unit_leader }
		set_nationality = ROOT
	}
}

transfer_all_unit_leaders_to_FROM = {
	every_unit_leader = {
		limit = { has_unit_leader_flag = AUS_anti_left }
		set_nationality = FROM
	}
}